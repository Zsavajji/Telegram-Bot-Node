-
	const title = "Telegram-Bot-Node";
	const description = "A plugin-based, all-in-one Telegram bot written in Node.js."
	const github = "https://github.com/crisbal/Telegram-Bot-Node";
	const repoName = github.split("/")[4];

doctype html
head
	meta(charset="utf-8")
	meta(http-equiv="X-UA-Compatible" content="IE=edge")
	meta(name="viewport" content="width=device-width, initial-scale=1")
	title #{title}
	link(rel="stylesheet" href="./bulma.min.css")
	link(rel="stylesheet" type="text/css" href="admin.css")
body
	nav.navbar.is-white
		.container
			.navbar-brand
				a.navbar-item.brand-text #{title}
			#navMenu.navbar-menu
				.navbar-start
					a.navbar-item(href=".")
						| Project page
					a.navbar-item(href=github)
						| Source code
					a.navbar-item(href=github + "/issues")
						| Issues
					a.navbar-item(href=github + "/pulls")
						| Pull requests
	.container
		section.hero.is-info.welcome.is-small
			.hero-body
				.container
					h1.title
						| #{title}
					h2.subtitle
						| #{description}
		// br
		// h1.title What is #{title}?
		p
			| #{title} is an extensible #[strong Telegram bot] written in Node.js. Its architecture is based on plugins, many of which are bundled by default. It features:
			//- .content is needed for bullet lists
			.content
				ul(style="list-style-type: square;")
					li All bot API functions
					li Advanced moderation tools
					li An installation wizard
					li Enabling, disabling and live-reloading plugins
					li Importing custom plugins
					li Inline messages
		br
		.columns
			a.column(href="user.html" style="padding: 0 0.75rem 0 0;"): .bd-notification(style="text-align: center; background-color: #4FC3F7")
					span.title Are you a user?
					p.
						Browse the usage guide
			a.column(href="developer.html" style="padding: 0 0 0 0.75rem;"): .bd-notification(style="text-align: center; background-color: #4FC3F7")
					span.title Are you a developer?
					p.
						Browse the documentation